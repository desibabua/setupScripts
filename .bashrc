source ~/bin/command

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
alias gst = "git status"
alias nodes = "node-prototype-repl"

# added by travis gem
[ ! -s /Users/ayushkumar/.travis/travis.sh ] || source /Users/ayushkumar/.travis/travis.sh
